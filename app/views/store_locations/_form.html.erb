<% if @store_location.errors.any? %>
   <div id="error_explanation">
     <h2>
       <%= pluralize(@store_location.errors.count, "error") %> prohibited
       this article from being saved:
     </h2>
     <ul>
       <% @store_location.errors.full_messages.each do |msg| %>
         <li><%= msg %></li>
       <% end %>
     </ul>
   </div>
 <% end %>

  <p>
    <%= f.label :city %><br>
    <%= f.select :city_id, options_from_collection_for_select(City.all, :id, :name, @store_location.city_id) %>
  </p>

 <p>
   <%= f.label :name %><br>
   <%= f.text_field :name %>
 </p>

 <p>
   <%= f.label :lat %><br>
   <%= f.text_field :lat %>
 </p>

 <p>
   <%= f.label :lng %><br>
   <%= f.text_field :lng %>
 </p>

  <p>
   <%= f.file_field :logo %>
   <%= image_tag @store_location.logo.url(:thumb) %>
 </p>